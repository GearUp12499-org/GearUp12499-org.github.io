---
import { ClientRouter } from "astro:transitions";
import { SEO } from "astro-seo";
import Background from "../components/ui/Background.astro";
import Footer from "../components/ui/Footer.astro";
import Header from "../components/ui/Header.astro";
import "../styles/global.css";

interface Props {
  title: string;
  bgColor?: string;
  primaryBallColor?: string;
  secondaryBallColor?: string;
  gridColor?: string;
  diagonalColor?: string;
  vignetteColor?: string;
}

const {
  title,
  bgColor = "#0F1115",
  primaryBallColor,
  secondaryBallColor,
  gridColor,
  diagonalColor,
  vignetteColor,
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <SEO
      title={title}
      titleTemplate="12499 Gear Up"
      description="The official website of FTC team 12499 Gear Up."
      canonical="https://gearup12499-org.github.io/"
      noindex={false}
      nofollow={false}
      openGraph={{
        basic: {
          title: title || "12499 Gear Up",
          type: "website",
          image: "/thumbnail.png",
          url: "https://gearup12499-org.github.io/",
        },
        optional: {
          locale: "en_US",
          siteName: "12499 Gear Up",
          description:
            "The official website of FTC team 12499 Gear Up.",
        },
        image: {
          alt: "Gear Up Logo",
          width: 1200,
          height: 630,
          type: "image/png",
        },
      }}
      extend={{
        meta: [
          { name: "twitter:image", content: "/thumbnail.png" },
          { name: "twitter:title", content: title },
          {
            name: "twitter:description",
            content:
              "The official website of FTC team 12499 Gear Up.",
          },
          { name: "theme-color", content: bgColor },
          { name: "apple-mobile-web-app-capable", content: "yes" },
          { name: "apple-mobile-web-app-status-bar-style", content: "black" },
          { name: "author", content: "12499 Gear Up" },
          {
            name: "keywords",
            content:
              "robotics, ftc, first tech challenge, gear up, 12499, gear up 12499, gear up robotics",
          },
        ],
        link: [
          {
            rel: "canonical",
            href: "https://gearup12499-org.github.io/",
          },
          { rel: "icon", type: "image/png", href: "/favicon.png" },
          { rel: "apple-touch-icon", href: "/favicon.png" },
        ],
      }}
    />
    <title>{title}</title>
    <ClientRouter />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://api.fontshare.com/v2/css?f[]=cal-sans@400,500,600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="m-0 min-h-screen relative font-sans"
    style={`background-color: ${bgColor}`}
  >
    <Background
      primaryBallColor={primaryBallColor}
      secondaryBallColor={secondaryBallColor}
      gridColor={gridColor}
      diagonalColor={diagonalColor}
      vignetteColor={vignetteColor}
    />
    <Header />
    <slot />
    <div class="mb-32">
      <Footer />
    </div>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
  }
</style>
